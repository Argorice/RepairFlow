{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "comment": "SPA: прямая ссылка на /orders/<id> должна открываться, а не отдавать 404",
      "source": "/((?!assets/).*)",
      "destination": "/index.html"
    }
  ]
}
